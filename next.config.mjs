/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['firebasestorage.googleapis.com', 'ai-statics.freepik.com', 'res.cloudinary.com']
  }
};

export default nextConfig;
